export enum ContainerDependencies {
  LOGGER = 'logger'
}

export enum ErrorCodes {
  SUCCESS = 200,
  UNAUTHORIZED = 401
}

export enum HttpVerb {
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT',
  PATCH = 'PATCH',
  DELETE = 'DELETE'
}
